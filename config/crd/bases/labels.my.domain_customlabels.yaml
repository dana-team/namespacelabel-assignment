---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: customlabels.labels.my.domain
spec:
  group: labels.my.domain
  names:
    kind: CustomLabels
    listKind: CustomLabelsList
    plural: customlabels
    singular: customlabels
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.applied
      name: Applied
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CustomLabels is the Schema for the customlabels API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CustomLabelsSpec defines the desired state of CustomLabels
            properties:
              customLabels:
                additionalProperties:
                  type: string
                description: map of custom labels to add to namespace
                type: object
            type: object
          status:
            description: CustomLabelsStatus defines the observed state of CustomLabels
            properties:
              applied:
                description: TRUE if labels have been added, else FALSE
                type: boolean
              message:
                type: string
            required:
            - applied
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
