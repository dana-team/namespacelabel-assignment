---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: customlabels.labels.dvir.io
spec:
  group: labels.dvir.io
  names:
    kind: CustomLabel
    listKind: CustomLabelList
    plural: customlabels
    singular: customlabel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.applied
      name: Applied
      type: boolean
    name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CustomLabelSpec defines the desired state of CustomLabel
            properties:
              customLabels:
                additionalProperties:
                  type: string
                description: 'CustomLabels: Map of custom labels that are added to
                  namespace. The actual name of the label in the namespace will be
                  crdname/labelname'
                type: object
            type: object
          status:
            description: CustomLabelStatus defines the observed state of CustomLabel
            properties:
              applied:
                description: 'Applied: The applied field will only be true if the
                  labels are added to the namespace Otherwise it will be false'
                type: boolean
              message:
                description: 'Message: Gives additional info regarding the customlabel
                  status or any error that occurred'
                type: string
              perLabelStatus:
                additionalProperties:
                  properties:
                    applied:
                      type: boolean
                    value:
                      type: string
                  required:
                  - applied
                  - value
                  type: object
                description: 'PerLabelStatus: Information regarding each label in
                  the spec, whether it was applied and the previous value'
                type: object
            required:
            - applied
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
